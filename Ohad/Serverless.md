 AWS lambda - instead of worrying about provisioning instances and maintaing them and configuring their networks, it allows to just execute pure code (function) in a blackbox which controls [[ec2]] instances. 

[[Opensearch]]

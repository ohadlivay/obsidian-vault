store anything, with metadata and identifier, access from anywhere via http API's (like amazon s3). good for backups, media, logs, big data.
pros:
* infinitely scalable
* rich metadata
* accessible everywhere
cons:
* slower
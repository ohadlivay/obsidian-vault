Eucledian representation: objects that exist in a regular or grid-like domain with a coordinate system.
* neighborhood is fixed.
* global coordinate - properly defined directions. 
* distance is trivial
cnns work for such use-cases because we can define a filter (neighbors sampled) and movement (up and down)

no eucledien does not 


tasks: 
	node classification
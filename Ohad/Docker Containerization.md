(needed for HW)
started as a solution for 'works on my machine' 
JVM tried to solve it (write once run everywhere)

# Containerization
lightweight from of virtualization that packages applications with all their dependence into a standardized unit called 'container'. each docker 'feels' alone. 
Cotainers dont need a guest OS unlike VM's, lower overhead.

## Docker
Architecture:
![[Pasted image 20251202172527.png]]
Image: template for creating a container
Container: an runnable instance of an image. all packed and ready.
Registry: storage for images like github